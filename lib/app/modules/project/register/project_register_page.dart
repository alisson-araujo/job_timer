import 'package:flutter/material.dart';

class ProjectRegisterPage extends StatelessWidget {
  const ProjectRegisterPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(child: Text('nandeska')),
    );
  }
}
